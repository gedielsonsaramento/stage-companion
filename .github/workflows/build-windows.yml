name: Build Stage Companion

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Source restoration in progress
        shell: powershell
        run: Write-Host 'Stage Companion source tree is being restored. Run manually after the next repository update.'
